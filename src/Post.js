class Post {
    constructor(img, headline, effectiveFrom, effectiveTo, body) {
      this.img = img;
      this.headline = headline;
      this.effectiveFrom = effectiveFrom;
      this.effectiveTo = effectiveTo;
      this.body = body;
    }
}
export default Post;